import 'package:flutter/material.dart';

class AppColors {
    static const primary = Color.fromARGB(255, 52, 121, 40); // Set warna primary secara eksplisit
    static const secondary = Color.fromARGB(255, 192, 235, 166); // Set warna secondary secara eksplisit
    static const onPrimary = Color.fromARGB(255, 255, 251, 230); // Set warna teks pada primary secara eksplisit
    static const onSecondary = Color.fromARGB(255, 255, 255, 255); 
}